export default class Person {
  constructor(id, displayname, members, is_team) {
    this.id = id;
    this.displayname = displayname;
    this.members = members;
    this.is_team = is_team;
  }
}
